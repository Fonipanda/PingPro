# PingPro backend configuration

# Optional: comma-separated list of allowed CORS origins
CORS_ORIGINS=*

# Optional: MongoDB URL (if not provided, in-memory storage is used)
# MONGO_URL=mongodb://localhost:27017
# DB_NAME=pingpro

# Optional: OpenAI API key for pose report generation
# OPENAI_API_KEY=your_key_here
